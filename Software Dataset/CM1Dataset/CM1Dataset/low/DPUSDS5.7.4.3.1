ADC Driver Functions	Updates the next entry in the array of ADC_VALUEs.  The function must be called twice to update a single value.  “Even” calls read the result of the prior conversion and setup the multiplexer for the next value.  “Odd”calls start the conversion setup in the previous call.